#include < stdio.h>

#define ADD '+'
#define SUBTRACT '-'
#define MULTIPLY '*'
#define DIVIDE '/'

#define MAX 4
typedef char* set;

int main () {
        long long int key;
        set operations[MAX] = {ADD, SUBTRACT, MULTIPLY, DIVIDE};
        scanf ("%lli", &key);

        if (key % 4 == 0) {

        } else 


    return 0;
    
}